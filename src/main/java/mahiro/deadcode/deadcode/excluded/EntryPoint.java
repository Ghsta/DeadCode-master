package mahiro.deadcode.deadcode.excluded;

import classes.DeadCode;

public class EntryPoint {
    public static void begin(String[] stringArray) {
        new DeadCode().iff(stringArray[0]);
    }
}

